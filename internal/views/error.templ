package views

templ ErrorPage(message, detail string) {
	@Layout(LayoutData{Title: "Error"}) {
		<section class="error-page">
			<h1 class="error-page__title">{ message }</h1>
			<pre class="error-page__body">{ detail }</pre>
			<a class="error-page__back" href="/">← back</a>
		</section>
	}
}
